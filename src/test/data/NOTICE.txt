The Data Folder is the only folder which should contains references to accessing the file system.
Because of this, it is limited in its testing ability. However, this means that the rest of the server
can be tested properly, by providing dummy data.